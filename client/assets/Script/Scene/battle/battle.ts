import { battleState } from '../../state/rootState'

const { ccclass, property } = cc._decorator

/**
 * @description
 * 订阅游戏状态流，并基于它来更新游戏的状态
 */
@ccclass
export class Battle extends cc.Component {
    onLoad() {
    }

    update() {
    }
}